When executing for the first time, run "./setup.sh" to install virtualenv, activating virtualenv, installing dependencies, and migrating database.

Activate virtualenv by running "source myvenv/bin/activate"

To run the server, run "python manage.py runserver"
